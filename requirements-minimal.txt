# Minimal requirements for Render free tier
# CPU-only PyTorch (smaller than CUDA version)

# Core System Dependencies
runpod>=1.6.0
psutil>=5.9.0

# Minimal PyTorch (CPU only - smaller footprint)
--index-url https://download.pytorch.org/whl/cpu
torch==2.5.0+cpu
torchaudio==2.5.0+cpu

# Essential audio processing
numpy>=1.26.0
librosa==0.11.0
soundfile>=0.12.0

# Story Narrator Dependencies (minimal subset)
transformers==4.46.3
diffusers==0.29.0
huggingface-hub>=0.20.0

# LLM
google-generativeai>=0.8.0

# Environment & Configuration
python-dotenv>=1.0.0
pydantic>=2.0.0

# Web Interface
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart

# Utilities
requests>=2.31.0
pyyaml>=6.0
